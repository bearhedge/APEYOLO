# APEYOLO Project Instructions

## Deployment Rules

**NEVER deploy to staging.** The staging environment has been deleted.

Always use `npm run deploy:prod` for deployments. Never use `npm run deploy:staging`.
